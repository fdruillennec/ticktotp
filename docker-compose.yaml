version: "3.8"

services:
  api:
    build:
      context: ./ticktotp-api
      dockerfile: Dockerfile
    container_name: totp-api
    ports:
      - "3000:3000"
    environment:
      - REDIS_ADDR=redis:6379
    volumes:
      - ./ticktotp-api:/app
    depends_on:
      - redis
    restart: unless-stopped
  
  frontend:
    build:
      context: ./ticktotp-frontend
      dockerfile: Dockerfile
      # Force rebuild by disabling cache
      args:
        BUILDKIT_INLINE_CACHE: 0
    container_name: totp-frontend
    ports:
      - "8080:80"
    environment:
      - VITE_API_URL=http://localhost:3000
    depends_on:
      - api
    restart: unless-stopped
    # Remove volumes to prevent caching issues

  redis:
    image: redis:7
    container_name: totp-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes", "--dir", "/data", "--appendfilename", "appendonly.aof"]
    restart: unless-stopped

volumes:
  redis-data:
    driver: local
